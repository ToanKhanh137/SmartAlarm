<?xml version="1.0" encoding="utf-8"?>
<!-- Layout dÃ¹ng chung cho Math/Shake/Squat/Step challenge -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:gravity="center"
    android:background="@color/ring_bg" android:padding="32dp">

    <TextView android:id="@+id/tvInstruction" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:textSize="18sp"
        android:textColor="@color/text_secondary" android:gravity="center"
        android:layout_marginBottom="32dp" android:lineSpacingMultiplier="1.4"/>

    <!-- Sá»‘ lá»›n giá»¯a mÃ n hÃ¬nh (cÃ¢u toÃ¡n hoáº·c sá»‘ Ä‘áº¿m) -->
    <TextView android:id="@+id/tvQuestion" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:textSize="56sp"
        android:textColor="@color/text_primary" android:gravity="center"
        android:fontFamily="sans-serif-light" android:layout_marginBottom="24dp"/>

    <!-- Progress bar -->
    <ProgressBar android:id="@+id/progressBar" style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent" android:layout_height="8dp"
        android:progressTint="@color/accent" android:progressBackgroundTint="@color/bg_card"
        android:layout_marginBottom="16dp"/>

    <!-- Progress text -->
    <TextView android:id="@+id/tvProgress" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:textSize="16sp"
        android:textColor="@color/accent" android:gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- Sá»‘ láº§n sai (Math) -->
    <TextView android:id="@+id/tvAttempts" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:textSize="13sp"
        android:textColor="@color/color_warning" android:gravity="center"
        android:layout_marginBottom="16dp" android:visibility="gone"/>

    <!-- Hint text (Shake/Squat/Step) -->
    <TextView android:id="@+id/tvHint" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:textSize="14sp"
        android:textColor="@color/text_hint" android:gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- Input answer (Math) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilAnswer"
        android:layout_width="200dp" android:layout_height="wrap_content"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_marginBottom="16dp">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etAnswer" android:layout_width="match_parent"
            android:layout_height="wrap_content" android:inputType="numberSigned"
            android:textColor="@color/text_primary" android:textSize="24sp"
            android:gravity="center" android:hint="?" android:maxLength="6"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Submit button (Math) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSubmit" android:layout_width="200dp" android:layout_height="52dp"
        android:text="Kiá»ƒm tra" android:backgroundTint="@color/accent"
        android:textColor="@color/text_primary" app:cornerRadius="12dp"
        xmlns:app="http://schemas.android.com/apk/res-auto"/>

</LinearLayout>

